git> 文档：`ModuleC\_FullUpdateLog.md`

> 开发成员：C

> 更新日期：2026-09-14

> 项目：番茄钟管理系统 PomodoroDB

> 负责四大模块：\*\*番茄计时模块、记录查询模块、数据统计模块\*\*



\## 一、本次全部更新内容（四大模块完整汇总）



\### 模块 1：番茄计时模块（核心业务）



✅ 实现功能



1\. 倒计时模式番茄钟，默认 25 分钟，启动时展示任务信息、总时长与操作提示

2\. 交互逻辑：运行时按回车键暂停计时；暂停后支持 3 条命令

&#x20;  - `p`：继续计时

&#x20;  - `a`：放弃本次专注，生成放弃记录入库

&#x20;  - `f`：手动结束，标记本次番茄为完成记录入库

3\. 多线程设计：计时线程 + 输入守护线程，控制台实时刷新剩余时间

4\. 自动计算真实专注秒数，自动填充`start\_time`、`end\_time`、`duration`、任务 ID、用户 ID、状态，写入`pomodoro\_records`数据表

5\. 状态区分：`status=1`代表完成记录；`status=0`代表放弃记录

6\. 计时结束后控制台输出本次专注时长，区分完成 / 放弃提示

7\. 关联任务表：番茄完成后自动更新对应任务的完成次数



\### 模块 2：记录查询模块



✅ 实现功能



1\. 菜单入口：【3. 记录查询】，提供 6 种查询入口

&#x20;  1. 按日期查询番茄记录（`yyyy-MM-dd`格式，增加输入预处理，去除首尾空格，非法日期友好提示）

&#x20;  2. 按任务查询番茄记录，输入任务编号，筛选该任务全部番茄记录

&#x20;  3. 按标签查询番茄记录，根据任务标签筛选对应记录

&#x20;  4. 查看全部番茄记录，列出当前用户所有计时记录

&#x20;  5. 查询未完成任务：列出存在放弃番茄记录的任务清单

&#x20;  6. 查询未完成天数：统计无有效完成番茄的日期

2\. 读取`pomodoro\_records`表数据，格式化展示记录信息

3\. 输入校验：非数字输入拦截，提示输入合法整数

4\. 时间解析容错，日期字符串自动 trim，严格校验日期合法性



\### 模块 3：数据统计模块



✅ 实现功能



1\. 菜单入口：【4. 统计】，读取数据库聚合用户番茄数据

2\. 统计指标：

&#x20;  - 总任务完成次数：统计 status=1 的有效完成番茄数量

&#x20;  - 本周任务完成次数：本周范围内，status=1 的完成番茄数量

&#x20;  - 总专注时长：汇总所有完成记录的 duration，换算为时、分展示

&#x20;  - 本周专注时长：本周完成记录专注时长汇总

&#x20;  - 未完成次数：统计放弃的番茄记录条数

3\. 自动过滤规则：\*\*放弃记录不计入完成次数、总专注时长\*\*，仅完成记录参与统计计算

4\. 从`pomodoro\_records`读取数据，实时聚合计算并控制台展示统计面板
数据库：PomodoroDB



1\. `pomodoro\_records` 番茄记录表



表格



| 字段 | 说明 |

| --- | --- |

| id | 记录主键 ID |

| user\_id | 用户编号 |

| task\_id | 任务编号 |

| start\_time | 计时开始时间 |

| end\_time | 计时结束时间 |

| duration | 专注时长，单位：秒 |

| status | 状态，1 = 完成，0 = 放弃 |

2\. 关联表：`tasks`任务表、`task\_daily\_records`任务每日记录表



> 

> 测试阶段产生的错误测试数据 R001、R002、R003 已执行 SQL 删除清理



\## 三、整体测试情况



测试环境：SQL Server 数据库 + Java 控制台项目

测试用例覆盖四大模块：



1\. 番茄计时：启动倒计时、暂停、继续、放弃、手动完成，验证时长入库准确，状态标记正确

2\. 记录查询：全部记录查询、按日期查询、按任务查询、未完成任务查询，验证输入校验、日期格式校验

3\. 统计模块：多次番茄计时，完成记录计入统计，放弃记录不参与时长与次数统计

