# 结构化的记忆

## 结构化的记忆

### 用户

- 小组课程项目成员，组队 D，负责统计与备份恢复模块的开发。

### 反馈

- 修改范围严格受限：只允许改动自己负责的 4 个文件（StatsService、BackupService、StatsMenu、BackupMenu），其他文件一律不动。
- 接口冻结：实体类字段、DAO 接口方法签名一律不许改；`UserDao` 没有 `insertWithId`，恢复用户改为 Service 内直接 SQL 按原 ID 插入。
- 统计口径固定：只计 FINISHED 记录、周口径为周一至周日、统计不加 deleted 过滤、展示用词用"未完成"不用"逾期"。
- 提交前需先 `git pull` 同步队友的改动，再提交推送。

### 项目

- [2026-09-12 18:20:53 小组课程项目（番茄任务管理系统：纯 Java 11 + JDBC + SQL Server，无框架）：本用户组队D，负责任务模块 4 个文件（TaskDaoImpl、TaskDailyRecordDaoImpl、TaskService、TaskMenu）]
- [2026-09-12 19:45:30 番茄任务管理系统——统计与备份恢复模块：负责 4 个文件（StatsService、BackupService、StatsMenu、BackupMenu）。统计：完成次数/专注时长只计 status=0 记录，周口径用 DateUtil.getWeekStart/getWeekEnd，未完成数 = 已过截止未达标的按次数任务 + overdue 每日记录，不加 deleted 过滤；备份恢复：Java 序列化单文件快照，恢复先删子表再按父表顺序以原 ID 插回，保证外键不断；恢复前强制 y/n 二次确认。]

### 参考文献

- 项目仓库：<https://github.com/MapleSuger4431/PomodoroTaskManager>
- 数据库建表脚本：`sql/schema.sql`（PomodoroDB，users / tasks / task_daily_records / pomodoro_records 四张表）
- JDK 11 文档：<https://docs.oracle.com/en/java/javase/11/docs/api/>
- Microsoft JDBC Driver for SQL Server：<https://learn.microsoft.com/sql/connect/jdbc/microsoft-jdbc-driver-for-sql-server>
