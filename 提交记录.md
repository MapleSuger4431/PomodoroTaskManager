# 提交记录

## 2026-09-12 用户模块（A 模块）完成记录【补记】

> 说明：用户模块代码完成时未同步编写提交记录文档，此处合并补记到本次提交中。

- **提交主题**：实现用户模块: UserDaoImpl + UserService + ConsoleMenu
- **修改文件**：
  - `src/main/java/com/pomodoro/dao/impl/UserDaoImpl.java`
  - `src/main/java/com/pomodoro/service/UserService.java`
  - `src/main/java/com/pomodoro/ui/ConsoleMenu.java`

### 功能说明

1. **UserDaoImpl**：基于 JDBC `PreparedStatement` 实现 `users` 表的增删改查
   （`findById` / `findByUsername` / `findAll` / `insert` / `update` / `deleteById` / `deleteAll` / `generateId`）。
   - `generateId()` 通过 `MAX(CAST(SUBSTRING(id, 2, LEN(id)) AS INT))` 生成 `U001` 格式 ID，表为空时返回 `U001`，方法加 `synchronized` 防止并发取号冲突。
   - `insert` 内部生成 ID 并回填实体，`create_time` 以 `new Date()` 写入。
   - `findAll` 无数据时返回空 List，不返回 null。
   - 全部 SQL 使用占位符参数，杜绝字符串拼接注入风险。
2. **UserService**：
   - `register`：校验用户名/密码非空，校验用户名未注册，密码经 `HashUtil.sha256` 加密后入库。
   - `login`：按用户名查询并以 SHA-256 哈希比对密码，失败统一抛 `IllegalArgumentException("用户名或密码错误")`，成功后记录当前用户。
   - `getCurrentUser` / `logout` / `isLoggedIn`：会话状态管理。
3. **ConsoleMenu**：主循环按登录状态切换登录菜单与主菜单；主菜单 1-5 依次路由到任务管理、番茄计时、记录查询、统计、备份/恢复子菜单，0 退出登录；注册包含确认密码校验，注册/登录异常均捕获并友好提示。

### 验证结果（连接本地 SQL Server 实测）

- 空表插入首条用户自动生成 `U001`，密码为 64 位 SHA-256 十六进制串，`create_time` 正确写入。
- `findByUsername` / `findById` / `findAll` 查询结果正确。
- 重复用户名注册被拒绝（提示"用户名已存在"），错误密码登录被拒绝（提示"用户名或密码错误"）。
- 登录后 `isLoggedIn = true`，退出登录后 `isLoggedIn = false`。
- 测试数据验证后已删除，无残留。

## 2026-09-14 主菜单退出登录清屏优化（masterA 分支）

- **提交主题**：主菜单退出登录后先清屏再展示登录菜单
- **修改文件**：`src/main/java/com/pomodoro/ui/ConsoleMenu.java`

### 功能说明

- 主菜单 `case 0` 退出登录时，先调用新增的 `clearScreen()` 清空控制台，再打印"已退出登录。"提示，随后主循环在干净的屏幕上重新展示登录菜单。
- `clearScreen()` 采用双保险策略：
  1. 先输出 ANSI 清屏转义序列 `\033[H\033[2J`（IDEA 控制台等支持 VT 的终端直接生效）；
  2. 再调用系统清屏命令兜底（Windows 用 `cmd /c cls`，其他系统用 `clear`），清除不支持 ANSI 的终端上残留的转义字符。

### 验证结果

- 全项目 `javac` 编译通过。
- 运行后选择退出登录，屏幕内容被清除，"已退出登录。"与登录菜单展示在屏幕顶部。

## 2026-09-14 仓库维护：删除备份序列化文件并建立修复前回滚点

- **提交主题**：删除误提交的 backup_U001.ser，忽略 *.ser，保存当前完整状态作为 Bug 修复前的回滚存档
- **修改文件**：删除 `backup_U001.ser`、`.gitignore`、`提交记录.md`
- **说明**：备份功能生成的 .ser 序列化文件属于运行时产物，不应进入版本库；已从本地与远程仓库同步删除，并通过 .gitignore 忽略后续生成的所有 .ser 文件。本次提交同时同步了 Codely 工具的记忆/配置文件改动，作为后续 Bug 修复前的完整回滚点（如需回滚可执行 `git reset --hard` 到本提交）。

## 2026-09-15 Bug 修复：演示前高危/中危问题修复（masterA 分支，未合并 master）

- **提交主题**：修复计时输入残留、清屏乱码、DAO 静默失败、跨天进度、统计口径与日期解析共 7 项问题
- **修改文件**：
  - `src/main/java/com/pomodoro/service/TimerService.java`
  - `src/main/java/com/pomodoro/ui/ConsoleMenu.java`
  - `src/main/java/com/pomodoro/dao/impl/PomodoroRecordDaoImpl.java`
  - `src/main/java/com/pomodoro/ui/TimerMenu.java`
  - `src/main/java/com/pomodoro/service/StatsService.java`
  - `src/main/java/com/pomodoro/ui/RecordMenu.java`

### 功能说明

1. **计时输入残留（高危）**：`TimerService` 输入守护线程不再新建 `Scanner`，改为 `BufferedReader` + `System.in.available()` 非阻塞轮询；计时结束后线程随 running 标志自然退出，消除与 `ConsoleUtil` 全局 Scanner 竞争导致的"计时后第一次按键被吞"问题。
2. **清屏乱码（高危）**：`ConsoleMenu.clearScreen()` 弃用 ANSI 转义序列与系统清屏命令，改为与 `TaskMenu`/`BackupMenu` 一致的 50 空行推屏方案，IDEA 控制台、cmd、PowerShell 下均不产生乱码。
3. **DAO 静默失败（高危）**：`PomodoroRecordDaoImpl` 全部 9 处 `catch (SQLException)` 由 `printStackTrace` 后静默返回改为抛出 `RuntimeException`，与其他 DAO 行为一致，杜绝番茄记录静默丢失与 generateId 失败返回重复 R001 的连锁问题。
4. **跨天进度丢失（中危）**：`TimerMenu.show()` 进入时先调用 `refreshDailyRecords`，前一天创建的按日期任务在今天计时时当日进度正常累计。
5. **统计口径统一（中危）**：`StatsService.getUnfinishedCount` 改为与 `RecordMenu` 一致的口径（未删除、未达标且未标记完成的按次数任务 + 每日逾期记录条数），不再依赖截止时间，且不再统计已删除任务。
6. **真实开始时间（低危）**：番茄记录的 `start_time` 改为会话开始时的真实时间，不再由结束时间倒推，消除暂停时长造成的偏差与跨周统计误差。
7. **日期严格解析（低危）**：`RecordMenu.handleByDate` 增加 `setLenient(false)` 严格解析与空输入校验，拦截 `2026-13-45` 这类非法日期。

### 验证结果

- 全项目 `javac -encoding UTF-8` 编译通过。
- 待演示前冒烟：注册 → 建任务（倒计时 1 分钟）→ 计时（暂停/继续/完成）→ 统计 → 按日期查询 → 备份/恢复 → 退出登录清屏。


